# 身份证识别性别

Generated field shortcut (basekit), human-auditable source.

## Build & upload

```
npm install
npm run build   # type-check
npm run pack    # -> output/*.zip
```

Then upload the zip to your Feishu developer console (field-shortcut capability) and have an admin approve it.
